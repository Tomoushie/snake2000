# Build output (rebuild from source with csc.exe or the .NET SDK)
*.exe
*.pdb
*.dll
bin/
obj/

# Editor/tooling-specific local config (never commit secrets like API keys)
.vscode/
.continue/
*.csproj.user
